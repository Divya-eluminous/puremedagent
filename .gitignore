/public/hot
/public/storage
/storage
/storage/*.key
/vendor
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
.env
# Any environment file variant - backups, copies and per-environment files all
# contain credentials and must never be committed.
.env.*
!.env.example
*.env
.env.bak
.env.backup
.phpunit.result.cache
.htaccess
public/google-calendar
composer.lock
/bin
/.composer
/.config
/.bin
/assets
composer.json
# Everything under /tests stays out as before, except the AI assistant's own
# suites. Git cannot re-include a path whose parent directory is excluded, so
# the contents are excluded rather than the directory itself.
/tests/*
!/tests/ai-assistant/
/usertesting
/stubs
/docs/
/tests/
/test-results/